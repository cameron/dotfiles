#! /bin/bash

# squash the most recent commit into the penultimate,
# taking the most recent's commit message

set -e

commit=$(git log -n 1 --format=format:"%B")
git reset HEAD~1 --soft
git commit -m "$commit"
